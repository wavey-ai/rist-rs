[package]
name = "wavey-rist"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Safe Rust bindings for librist (RIST protocol)"

[lib]
name = "rist"

[features]
default = []
pure-rust = ["dep:rist-core", "dep:rist-mio"]
tokio = ["dep:tokio", "dep:libc"]

[dependencies]
rist-core = { version = "0.1.0", optional = true }
rist-mio = { version = "0.1.0", optional = true }
rist-sys = "0.1.0"
thiserror = "2"
tokio = { version = "1", features = ["rt", "rt-multi-thread", "sync", "time", "io-util", "macros", "net"], optional = true }
libc = { version = "0.2", optional = true }

[[example]]
name = "sender"
required-features = ["tokio"]

[[example]]
name = "receiver"
required-features = ["tokio"]
