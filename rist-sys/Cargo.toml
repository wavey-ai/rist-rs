[package]
name = "rist-sys"
version = "0.1.1"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Raw FFI bindings to librist"
links = "rist"

[build-dependencies]
bindgen = "0.70"
pkg-config = "0.3"

[features]
default = []
